#include <stdio.h>
int main() 
{
    int Value,square;
    printf("Enter a number: ");
    scanf("%d", &inputValue);
    square = inputValue * inputValue;
    printf("The square of %d is: %d\n",Value, square);
    return 0;
}
