#include<stdio.h>
int main()
{
  int first,second,Division;
  printf("enter first number:");
  scanf("%d",&first);
  printf("enter second number:");
  scanf("%d",&second);
  Division=first/second;
  printf("Division of the value %d:",Division);
}
