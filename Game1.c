#include<stdio.h>
int main()
{
int a,b;
printf("enter a value:");
scanf("%d",&a);
printf("enter b value:");
scanf("%d",&b);
if(a>=0 && a<=2 && b>=0 && b<=2)
{
    if(a==b)
    {
    printf("match draw");
    }
    if else(a>b)
    {
    printf("a wins");
    }
    else
    {
    printf("b wins");
    }
}
else
{
printf("invalid");
}
}
